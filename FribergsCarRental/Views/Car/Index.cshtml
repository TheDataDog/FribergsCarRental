@model IEnumerable<FribergsCarRental.Models.Car>

@{
    ViewData["Title"] = "Index";
}

<h1 style="text-align:center;">Våra Bilar</h1>

<hr />
<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            if (item.Pictures != null && item.Pictures.Any())
            {
                <div class="col-md-4 col-xl-3 col-sm-6 mb-4">
                    <div class="d-flex justify-content-center align-items-center">
                        <div class="card" style="width: 18rem;">
                            <img src="@item.Pictures[0]" class="card-img-top" alt="car">
                            <div class="card-body">
                                <h5 class="card-title">@item.Brand @item.Model @item.YearModel</h5>
                                <div class="d-flex justify-content-between mb-2">
                                    <p class="card-text d-flex w-100 justify-content-between">
                                        <span class="d-flex align-items-center">
                                            <i class="fa-solid fa-user"></i>&nbsp;@item.PassengerSeats
                                        </span>
                                        <span class="d-flex align-items-center">
                                            <i class="fa-solid fa-gas-pump"></i>&nbsp;@item.FuelConsumption l/100km
                                        </span>
                                        <span class="d-flex align-items-center">
                                            <i class="fa-solid fa-horse"></i>&nbsp;@item.HorsePower hk
                                        </span>
                                    </p>
                                </div>
                                <p style="font-size:1.3em; font-weight:bold">
                                    @item.DailyCost kr/dygn
                                </p>
                                <a asp-action="ShowCar" asp-route-id="@item.CarId" class="btn btn-dark">Läs mer & boka</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>

@* <table class="table">
    <thead>
        <tr>
            @* <th>
                @Html.DisplayNameFor(model => model.Pictures)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YearModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HorsePower)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FuelConsumption)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PassengerSeats)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @* <td>
                    @Html.DisplayFor(modelItem => item.Pictures)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.YearModel)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HorsePower)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FuelConsumption)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PassengerSeats)
                </td>
                <td>
                    @* <a asp-action="Edit" asp-route-id="@item.CarId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.CarId">Detaljer</a> |
                    <a asp-action="Book" asp-route-id="@item.CarId">Boka</a>
                    @* <a asp-action="Delete" asp-route-id="@item.CarId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@
